version: '3.8'

services:
  socket-proxy:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - socket-proxy-recordings:/app/recordings
    command: ["./socket-proxy", "--auto-connect=true"]

volumes:
  socket-proxy-recordings: